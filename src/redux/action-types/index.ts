export enum AuthActionType {
  SIGNIN = 'signIn',
  SIGNOUT = 'signOut',
  LOGGING = 'logging',
}

export enum VideoCardsType {
  PLAY = 'play',
  PAUSE = 'pause',
}

export enum ProfileActionType {
  UPDATE = 'update',
}
